server:
    port: 8088
spring:
  application:
      name: fraude
  datasource:
      password: password
      url: jdbc:postgresql://localhost:5432/fraude
      username: ekommerce
  jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true
